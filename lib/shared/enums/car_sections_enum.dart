enum CarSectionEnum {
  leftSideHealth('leftSideHealth'),
  rightSideHealth('rightSideHealth'),
  frontHealth('frontHealth'),
  rearHealth('rearHealth');

  const CarSectionEnum(this.value);
  final String value;
}
